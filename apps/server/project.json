{
    "name": "blockchain-server",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "projectType": "application",
    "sourceRoot": "apps/server",
    "tags": [],
    "targets": {
        "build": {
            "executor": "@nx/hardhat:build",
            "options": {
                "port": 8000,
                "hostname": "172.18.43.154"
            }
        },
        "serve": {
            "executor": "@nx/hardhat:serve",
            "dependsOn": [
                "^build"
            ]
        }
    }
}
